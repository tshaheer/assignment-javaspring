logging:
  level:
    web: DEBUG
    sql: DEBUG

server:
  port: 8080
  servlet:
    context-path: /api

spring:
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:ucanaccess://C:/assignment/accountsdb.accdb;openExclusive=false;ignoreCase=true;showSchema=true
    driver-class-name: net.ucanaccess.jdbc.UcanaccessDriver
    username:
    password: